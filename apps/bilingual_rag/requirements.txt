# Bilingual RAG (Project 4) — converted from the Kaggle notebook.
# Chroma-only (Qdrant/Elasticsearch from the notebook are intentionally dropped).
chromadb>=0.5
sentence-transformers>=3.0   # BGE-M3 embedder + multilingual cross-encoder reranker
torch>=2.0
ollama>=0.2.0                 # chat with qwen2.5:3b-instruct
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
pydantic-settings>=2.0
gradio>=4.0
pandas
requests
